# Repository governance (always-on adapter)

The authoritative repository instructions are @../../AGENTS.md.

Before editing, read that file, the active task contract, and @../../docs/SOURCE_OF_TRUTH.md. This Antigravity rule is intentionally thin; do not copy architecture or security rules here and allow them to drift.

Verified adapter location: current Antigravity workspace rules live under `.agents/rules/` (legacy `.agent/rules/` remains supported). If activation is configurable in the Antigravity UI, set this rule to **Always On**.
