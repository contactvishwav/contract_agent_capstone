# Capstone Remediation — Consolidated Summary

*A retrospective of the full arc: inherited state → correctness bugs → real LLM extraction → enterprise-readiness remediation → live end-to-end verification → closing the README's remaining "future enhancement" gaps → a final real-browser verification. Every claim below is grounded in a commit hash, a file:line, or a benchmark results file — this document does not restate the aspirational claims in `README.md`/`docs/RESUME_WRITEUP.md` without checking them against the code.*

---

## 1. What was inherited

The repository's own marketing copy (`README.md`, `docs/RESUME_WRITEUP.md`) claimed an "enterprise-grade" multi-agent system: 11+ specialized agents, a Supervisor pattern with "quality gates, error recovery, and A-F grading," a standardized MCP interface "with full data isolation and tracing," "explainable AI reasoning," "95%+ accuracy," "100% alignment with company standards," and "reducing legal review time by 70%." None of these claims had been checked against running code before this engagement began.

What was actually true, verified by reading the code:

- **Stubbed extraction.** `LLMClauseExtractor`, `LLMCUADClassifier`, and `ClauseDetectorTool` all had stubbed `_parse_llm_response()` implementations that returned `[]` regardless of input — `ClauseDetectorTool` didn't call the LLM at all. Locked in by a regression test (`test_stubbed_llm_parsers.py`, commit `66fb146`) before it was fixed, specifically so the eventual fix would be provably a fix rather than a silent behavior change.
- **Three parallel orchestration systems, not one.** The traditional LangGraph workflow (`IntelligenceOrchestrator`), the planning agent (`PlanExecutionEngine`, the actual default via `use_planning=True`), and `SupervisorAgent` (reachable only via the separate `POST /api/supervisor/workflow/execute` route) all exist independently. `RetryManager`/`CircuitBreakerManager` are only ever invoked inside `SupervisorAgent._execute_step_with_protection` — the actual default analysis path has zero reference to either class. The "quality gates" and "error recovery" advertised in the README exist, but not on the path that runs by default.
- **Broken MCP tool.** `fetch_contract_metadata` called an async repository method without `await`-ing it, so it always returned an unawaited coroutine that failed `json.dumps()` on every call. Existing tests used `MagicMock` instead of `AsyncMock`, which meant the bug was invisible to the test suite (fixed `e6c0eb1`).
- **Broken Supervisor wiring, two layers deep.** `SupervisorAgentFactory`, `PolicyWorkflowOrchestrator`, and `PolicyWorkflowSupervisor` all constructed `SupervisorAgent` with the wrong argument count — it was never successfully instantiated, likely crashing `POST /api/policies/upload` on every call (fixed `96f7250`). Fixing construction surfaced two more never-exercised bugs in the same file (a nonexistent `AuditEvent` class, a `ContentValidator` signature mismatch), fixed in the same commit. Even after construction was fixed, neither orchestrator class ever calls `self.supervisor.coordinate_workflow(...)` — the quality-gate/retry/circuit-breaker machinery is no longer *broken*, it's simply **never called**, on the one route that was fixed to construct it correctly.

This picture — stubbed extraction, disconnected orchestration systems, a crashing MCP tool, and dead-on-arrival Supervisor wiring — is the actual starting point this whole engagement worked from, not the README's description of it.

---

## 2. Phase 0 — correctness bugs

Four bugs, each following the same discipline: identify → write/confirm a regression test that fails on the current code → fix → verify the test now passes and nothing else regressed.

| # | Bug | Regression test | Commit |
|---|---|---|---|
| 1 | `fetch_contract_metadata` (`backend/mcp_server.py`) called `get_contract_by_id` (an async repo method) without `await` — always returned an unawaited coroutine, failing `json.dumps()`. Existing tests masked it with `MagicMock` instead of `AsyncMock`. | Converted `test_mcp_capabilities.py`'s mocks to `AsyncMock`; added a regression test; verified failure on pre-fix code via `git stash`. | `e6c0eb1` |
| 2 | `SupervisorAgent` constructed with the wrong argument count at all 3 call sites (`factory.py`, `policy_workflow_orchestrator.py`, `policy_workflow_supervisor.py`) — never successfully instantiated, likely crashing `POST /api/policies/upload` on every call. Fixing it surfaced 2 more never-exercised bugs (`AuditEvent` didn't exist; a `ContentValidator` signature mismatch). | `test_supervisor_construction.py` (4 tests), covering all 3 sites plus a `PolicyService()` smoke test. | `96f7250` |
| 3 | `search_prior_approved_clauses` queried a `[:CONTAINS]` relationship, but ingestion actually creates `[:CONTAINS_CLAUSE]` — precedent search always returned zero real results and silently fell through to a mock-data fallback. | `test_precedent_matcher_relationship.py` (2 tests), using a fake graph mirroring real ingestion's relationship type; confirmed 0 results pre-fix. | `d0d2860` |
| 4 | `LLMClauseExtractor`/`LLMCUADClassifier`/`ClauseDetectorTool` all had stubbed `_parse_llm_response()` returning `[]` unconditionally — `ClauseDetectorTool` never called the LLM at all. | `test_stubbed_llm_parsers.py` (3 tests, `TODO(Phase 1)` markers) — a deliberate test-first commit with no source fix, so Phase 1's fix would be provably a fix. | `66fb146` (test) → `e6553cb` (fix, Phase 1) |

Full suite at the end of Phase 0/1: 8 failed / 42 passed / 1 skipped — unchanged from the pre-Phase-0 baseline, confirming zero regressions from any of these four fixes (the 8 pre-existing failures are addressed later; see §4/§6).

---

## 3. Phase 1 — real LLM extraction

`e6553cb` replaced all three stubs with a single `LLMExtractionService` using Gemini structured output against a verified 41-type CUAD taxonomy (cross-checked against the `theatticusproject/cuad-qa` dataset, which also fixed a duplicate and a wrong category name in the old `CUAD_CLAUSE_TYPES` constant). Offsets are computed deterministically by searching the source text for the LLM's claimed `extracted_text`, not trusted from the model — `start_offset == -1` signals text that couldn't be located verbatim (a hallucination-risk flag, acted on later in §4). The 6,000-character truncation that had been silently dropping late-appearing clauses was removed entirely.

**The 497-contract Flash-Lite benchmark** (`research/benchmark/extraction_eval_results_flash-lite-497.json`), scored against real CUAD ground truth on the 5 metadata columns then available:

| Type | Precision | Recall | F1 |
|---|---|---|---|
| Document Name | 1.00 | 0.99 | 0.99 |
| Parties | 1.00 | 0.95 | 0.98 |
| Agreement Date | 0.98 | 0.63 | 0.77 |
| Effective Date | 0.90 | 0.65 | 0.76 |
| Expiration Date | 0.91 | 0.15 | 0.26 |

Root-cause breakdown on the weakest column (Expiration Date): of 274 misses, 96% (263) were the model finding nothing at all for that contract; only 4% (11) were found-but-unmatched — confirming the date-matching logic itself works, and the gap is model recall, not the matcher.

**The quota-forced model substitution**: this full-scale run used `gemini-flash-lite-latest`, not the production model `gemini-2.5-flash`, because the Gemini free tier caps `gemini-2.5-flash` at 20 requests/day/project — nowhere near enough for a 497-contract run. This substitution is explicit in every benchmark artifact's `model` field, not silently absorbed into "the benchmark."

**The gemini-2.5-flash confirmation sample** (metadata-only, stratified toward the weak date types — `research/benchmark/extraction_eval_results_gemini-2.5-flash-stratified.json`): as of this report, **44 of 90 contracts complete**, `stopped_early: true` (the same real 20/day free-tier quota wall). Numbers so far, on the production model:

| Type | Precision | Recall | F1 |
|---|---|---|---|
| Document Name | 1.00 | 1.00 | 1.00 |
| Parties | 1.00 | 1.00 | 1.00 |
| Agreement Date | 0.91 | 0.79 | 0.84 |
| Effective Date | 0.77 | 0.87 | 0.82 |
| Expiration Date | 0.76 | 0.48 | 0.59 |

Directionally consistent with the Flash-Lite root-cause analysis: the production model is meaningfully better on the weak date fields (Expiration Date F1 0.26 → 0.59) without changing the matching logic — confirming the earlier diagnosis that the gap was model capability, not the matcher. A separate, later confirmation sample scoring the *risk-relevant* categories on gemini-2.5-flash (as opposed to just these 5 metadata columns) is covered in §4, since risk-category benchmarking was itself a later punch-list item (P2 item 12) — it is currently at 5/90, blocked on the same quota mechanism (checked directly against the recurring benchmark job's checkpoint file as of this report).

---

## 4. P0–P3 — enterprise readiness remediation

This phase followed a from-scratch, file:line-grounded audit (`docs/ENTERPRISE_READINESS.md`), rating 9 domain-specific areas — explainability, accuracy, tenant isolation, reliability, scalability, observability, data integrity, testing/deployment, and cost — against what a legal/compliance team actually needs (defensibility, traceability, tenant-safe confidentiality), not a generic SaaS checklist. All 9 areas started 🔴 or 🟡; the punch list below is ordered by that audit's own priority.

### P0 — severe risk, low-to-medium effort

1. **RBAC default-to-ADMIN bug** (`backend/governance/rbac.py:56-59`). The code's own comment claimed "default to VIEWER for safety" — it actually returned `UserRole.ADMIN` on a missing `X-User-Role` header, a live privilege-escalation path granting full permissions including delete rights to any unauthenticated/misconfigured request. Fixed `2cc284f`.
2. **Cross-tenant read/delete path in `PolicyRepository`.** `get_policy_by_id`, `_get_policy_rules`, `update_policy_version`, `delete_policy` had zero `tenant_id` filter in their Cypher. Combined with bug #1, this was a *confirmed-reachable* exploit chain, not theoretical: `policy_id` is generated as `f"policy_{tenant_id}_{timestamp}"` (target tenant derivable from the ID itself), and two of the four methods were live behind real routes — `GET /{policy_id}` and `DELETE /{policy_id}` (`backend/api/policy_api.py`), gated only by a permission check that the RBAC bug let anyone bypass by simply omitting a header. All four methods tenant-scoped in `9771ee2`; delete/update also fixed to correctly report failure instead of always returning `True` when a tenant-scoped match finds nothing.
3. **`get_intelligence_status`/dashboard hardcoded `"default-tenant"`** regardless of caller — fixed to use the real caller's `tenant_id`, `b58931d`.
4. **`ChainOfThoughtAgent._risk_assessment_chain`'s `overall_risk` `NameError`** — referenced a variable never assigned, guaranteeing failure whenever CoT risk assessment ran. Fixed by computing it as the max severity-derived score across violations; a second bug (the success path never included a `pattern` key) surfaced and was fixed in the same pass. `39bf98c`.
5. **`AuditLogger.get_audit_trail`/`ErrorTracker.get_error_statistics` retrieval**, flagged by the original audit as confirmed-broken via failing tests. Investigation found no bug in the actual retrieval logic — both failures traced to a bare `MagicMock` from an unrelated test file's guard leaking into the shared Neo4j singleton (its default `__iter__` silently yields nothing), a collection-order artifact, not a real defect. Fixed by giving both tests a proper fake graph simulating real `MERGE`/`MATCH` semantics. `b4606d8`. Suite: 5 failed / 61 passed / 1 skipped / 1 error, down from 8/42/1/2.

### P1 — severe risk, medium effort

6. **`tenant_id` required, not defaulted**, on upload/analysis routes (`contracts.py`, `document_upload.py`, `enhanced_document_upload.py`) — previously silently defaulted to `"default-tenant"` on omission, letting a request read/write another tenant's bucket unnoticed. No real authenticated-identity layer exists to verify a *claimed* `tenant_id` belongs to the caller (see §6), so this scoped down to the buildable half: reject the request outright (422) rather than default. `ae7d400`, `test_tenant_id_required.py` (8 affected routes, git-stash-verified).
7. **Audit trail wired into the real analysis path**, threading `contract_id`/`tenant_id` end-to-end through *both* orchestration paths (previously dropped one layer below `analyze_contract_by_id`, never reaching either), instrumented once at the shared tool layer (`ClauseDetectorTool`, `PolicyCheckerTool`, `RiskCalculatorTool`, `RedlineGeneratorTool`) rather than duplicated per orchestration path.
8. **Clause-linked violations/risk**: a stable, deterministic `clause_id` (contract+type+offset derived, not random) threaded through `PolicyCheckerTool`'s violations and a new `critical_issue_details` field on `RiskCalculatorTool` (additive — the existing `critical_issues: List[str]` shape was left untouched since two frontend components render it directly).
9. **Honest partial-failure state**: `RiskCalculatorTool`'s except path no longer fabricates `50.0`/`"MEDIUM"`; `_generate_redlines`'s except path no longer marks `is_complete: True` on failure; `PlanExecutionEngine` tracks per-step `node_status` instead of always reporting `processing_complete: True`; the API surfaces both honestly instead of a hardcoded `analysis_complete: True`. Items 7-9 landed together as `2dd078e` (shared architectural prerequisite). Suite: 84 passed / 5 pre-existing-unrelated failed / 1 skipped, up from 69/5/1.

### P2 — medium-term

10. **Ungrounded-clause flagging**: `start_offset == -1` (Phase 1's hallucination-risk signal) was computed but never acted on. `ClauseDetectorTool` now sets a `grounded` flag per clause, threaded through violations/risk as `clause_grounded` and surfaced in the API response — flagged, not excluded, so a human reviewer can still see it but knows not to trust it at face value.
11. **Hybrid policy/risk engine**, replacing keyword-matching against a single hardcoded 6-category dict (2 of which were dead code for a CUAD-only pipeline): a small deterministic table for the 5 categories with an objective numeric threshold (Cap On Liability, Minimum Commitment, Notice Period To Terminate Renewal, Warranty Duration, Renewal Term — no LLM call, never guesses), plus `PolicyEvaluationService` for everything else (one LLM call per clause against a tenant's own uploaded `PolicyRule` set, or a labeled default set if none exists; every violation cites a real `rule_id`, independently verified against the exact set offered to the model — a hallucinated citation is discarded). `PolicyComplianceAgent` (the standalone compliance-check route) now delegates to this same engine instead of its own separate, more primitive matcher. Items 10-11: `7c6ecc6`, 10 new tests, suite 95/5/1 (up from 88).
12. **Risk-category extraction benchmark** — the headline accuracy finding of this whole engagement. The original benchmark validated only 5 metadata columns; the platform's actual value proposition (flagging risky clauses) had zero accuracy data. `prepare_risk_category_ground_truth.py` sourced real ground truth for the other 36 CUAD categories from the same dataset (497/510 contracts matched — same corpus gap as before). Full-scale result, Flash-Lite, 497 contracts (`research/benchmark/extraction_eval_results_risk-categories-flash-lite-497.json`):

    | | Avg Precision | Avg Recall | Avg F1 |
    |---|---|---|---|
    | 5 metadata columns | 0.92 | 0.68 | 0.75 |
    | 36 risk-relevant columns | 0.63 | 0.27 | 0.32 |

    5 of 36 risk categories scored a flat 0.00 F1 (Competitive Restriction Exception, Price Restrictions, Volume Restriction, Affiliate License-Licensor, Unlimited/All-You-Can-Eat-License); 20 of 36 scored below 0.30. Only 3 (Governing Law 0.98, No-Solicit Of Employees 0.82, Anti-Assignment 0.81) matched metadata-tier quality. Recall is the dominant gap — clauses missed, not fabricated. **Confirms the audit's own concern directly: accurate metadata extraction does not imply accurate risk extraction**, and until this pass, that gap had never been measured. A gemini-2.5-flash confirmation sample of this same 36-category scoring is in progress (`risk-categories-2.5-flash` run) — currently 5/90, `stopped_early: true`, blocked on the per-minute free-tier quota (confirmed via the actual `429`/`GenerateRequestsPerMinutePerProjectPerModel-FreeTier` response in the run log, not assumed). `2636751`.
13. **Rate limiting / rate-limit-aware retry.** A shared `threading.Semaphore` (`backend/shared/utils/llm_concurrency.py`, env-configurable, default 5) wired into both LLM services' outbound calls; `StepExecutor._execute_step_with_retry` now catches `ResourceExhausted` specifically and fails fast instead of retrying into it (ordinary transient failures still retry with backoff). `7d025e0`.
14. **`Contract.file_id` uniqueness constraint + migration consolidation.** Exactly 4 constraints existed repo-wide, none on `Contract`, despite `store_contract` using `CREATE` (not `MERGE`). New `run_all_migrations.py`: a single, versioned, idempotent runner tracking applied migrations via a `:SchemaMigration` node — a migration that raises is never marked applied, so a retry doesn't silently skip a broken state. `3604eaa`.
15. **`correlation_id`/`trace_id` bridging** across the HTTP/MCP process boundary — all 4 MCP tool functions now accept an optional `correlation_id`, preferred over generating a fresh, disconnected `trace_id`. Note in §6: this is a capability, not an automatic end-to-end wire-up (nothing in this codebase currently calls from FastAPI into the MCP server in-process). `68f829a`.

### P3 — infrastructure

16. **Native Neo4j vector index**, replacing brute-force `gds.similarity.cosine()`/`vector.similarity.cosine()` scans with `CREATE VECTOR INDEX` + `db.index.vector.queryNodes` across Contract/Section/Clause/Chunk/PolicyDocument embeddings. Benchmarked on 5,000 synthetic contract nodes: **~4.7x mean / ~7x median latency improvement** (`research/benchmark/vector_index_benchmark.py`).
17. **Local Neo4j + Redis in docker-compose** (replacing the default dependency on a shared public demo Neo4j instance), plus the actual root cause of `test_mcp_capabilities.py`'s long-standing collection failures: `test_ai_patterns.py`/`test_policy_system.py` each did `sys.path.insert(0, <backend/>)`, letting the local `backend/mcp/` package shadow the real third-party `mcp` SDK for the rest of the pytest process — a collection-order accident, not a missing-mock problem. Both dead `sys.path` hacks removed.
18. **CI/CD** (`.github/workflows/ci.yml`): a `test` job against a real Neo4j service container (removing the collection-order dependency on mocking luck) plus a `lint` job (ruff, blocking on E9/F821, advisory on the rest — surfaced 9 real F821 undefined-name bugs, several in security/audit-critical paths: the prompt-injection validator threw on every call, all four `PolicyAuditService` methods threw on every call). Two follow-up fixes: `[tool.pytest.ini_options] pythonpath = [".."]` in `backend/pyproject.toml` (CI's `working-directory: backend` broke absolute `backend.xxx` imports that only worked locally by accident — `b5bf892`), and `--ignore=tests/test_ai_patterns.py --ignore=tests/test_policy_system.py` for the 2 pre-existing async smoke scripts that have never had a working async-test setup (`9d9fe90`, tracked as punch-list item 22 rather than a silent exclusion, `f860dbc`). `9107fb5`.
19. **Production-grade Dockerfiles**, multi-stage (dev/production), non-root user, `fastapi run` not `fastapi dev`; frontend production serves the compiled static build via nginx with an `/api/` reverse-proxy config (without which every API call from the built frontend would 404).
20. **LLM extraction caching + cost/usage monitoring.** `LLMExtractionService.extract_clauses`/`PolicyEvaluationService.evaluate_clause` (the only two real LLM call sites) now cache on a hash of prompt version + model + inputs, finally making `CACHE_ENABLED` gate something real. New `LLMUsageTracker`, surfaced at `GET /api/monitoring/llm-usage`.
21. **Encryption at rest + PII extension to ingestion, across all 3 content locations.** `SecurityValidator`'s real bug fixed first: it set `is_valid=True` even when PII was found, making every hit invisible. New `backend/infrastructure/encryption.py` — AES-256-GCM field-level encryption (via `IKeyProvider`/`EnvKeyProvider`, swappable for a real secrets manager later) — applied in sequence to all three places contract text is actually stored:
    - `Contract.full_text` / `Clause.content` — redact-then-encrypt on write, decrypt on read (`1395b75`).
    - `Chunk.content` / `DocumentChunk.content` — the third copy, confirmed (not assumed) to have 6 live Cypher call sites doing in-Cypher `CONTAINS`/`substring()` directly on this content, including in the *primary* semantic-search path's snippet generation, not just a fallback. Encryption required restructuring those call sites to fetch a bounded candidate set and operate on decrypted content in Python instead, since Neo4j can't do string operations on ciphertext (`375e446`).

    Suite after item 21 (both parts): 211 passed / 5 pre-existing-unrelated failed / 1 skipped, verified via the literal CI invocation on a clean-room `uv sync --frozen` rebuild (211/0/1).

---

## 5. Live end-to-end verification

Everything above was validated by a mocked test suite. This section is deliberately separate, because a mocked suite proved insufficient on its own — it required a real pass with actual API endpoints, live Neo4j/Redis, and real Gemini calls to find what it structurally could not.

### The mocking audit came first, because the "everything's mocked" assumption was false

Before running anything live, an audit of `backend/tests/` (`bafe79d`) found two files silently riding on pytest's collection order rather than genuine isolation:

- `test_pattern_integration.py` had **no `Neo4jGraph` patching at all**. `TestChainOfThoughtAgent` transitively constructed a real `Neo4jGraph()` (via `ChainOfThoughtAgent._risk_assessment_chain`'s lazy `PolicyRepository` import), and `TestReACTAgent` made a **real, unmocked `ChatGoogleGenerativeAI().invoke()` call** whenever `GOOGLE_API_KEY` was set — failures were swallowed, so the test "passed" regardless, but the call was genuinely made. Both only worked because an earlier-alphabetical test file's mock happened to still be cached in `sys.modules`; run either class in isolation and it hit real infrastructure.
- `test_rbac_integrated.py` had zero mocking at all (`from backend.main import app` transitively builds a real `Neo4jGraph()` the same way).
- `backend/shared/cache/redis_cache.py`'s module-level `cache = RedisCache()` singleton always attempted a real `redis.from_url(...).ping()` on import; whether a test got the `InMemoryCache` fallback was environmental luck, not a deliberate choice.

Fixed by wrapping both files' imports in the standard `Neo4jGraph` patch (with `.query.return_value = []` explicitly configured — a bare mock's `.query()` isn't iterable, which would make `PolicyRepository.get_applicable_policies` re-raise and silently flip assertions), injecting a fake LLM into `ReACTAgent.clause_tool`, and adding `backend/tests/conftest.py` to patch `redis.from_url` at module scope for the whole suite. Verified both files pass in isolation with `GOOGLE_API_KEY` unset and `NEO4J_URI` unreachable — proving the collection-order reliance was actually gone.

### What the live walkthrough confirmed working

Against real Docker-composed Neo4j/Redis and the real Gemini API (no mocking in the request path):

- **Real encryption, genuine ciphertext.** A direct `cypher-shell` query of `c.full_text` on a live-uploaded contract returned base64-looking AES-256-GCM ciphertext — confirmed unreadable, not a mocked assertion.
- **Real caching preventing redundant calls.** Re-running analysis on the same contract showed `total_calls` incrementing with genuine `cache_hits` for the clauses that had succeeded the first time (0 new real Gemini calls for those), while the 2 clauses that had failed a real 429 on the first run — correctly *not* cached, since a failed call never reaches the cache-set step — made fresh real calls on the second run and succeeded.
- **Real cross-tenant isolation.** The same contract ID requested with a different tenant's `tenant_id`, on both `GET /status` and `POST /analyze`, returned a clean 404 on each — not a mocked assumption.

### The 4 real bugs the live walkthrough found that a 211-test, all-passing suite did not

1. **`PolicyEvaluationService.evaluate_clause` silently swallowing real failures into a false clean result.** A genuine `429 RESOURCE_EXHAUSTED` during a live analysis run produced `intelligence_status: "completed"`, `risk_level: "LOW"`, `violations_count: 0`, with a `"success"` audit entry — because `evaluate_clause`'s `except Exception: return []` was indistinguishable, to every caller upstream, from "the model genuinely found zero violations." Fixed (`d163822`) by making `evaluate_clause` raise instead of swallow, `PolicyCheckerTool._run` catch per-clause and track `failed_clause_ids`, and both orchestration paths propagate an honest `"partial"` node status through to `processing_complete`/the persisted `intelligence_status` (now `"completed_with_errors"` when appropriate).
2. **Two separate Neo4j `DateTime` serialization leaks**, beyond the one initially found in `AuditLogger.get_audit_trail`: `GET .../status`'s `last_updated` field (`contract_intelligence.py`) and `ErrorTracker.get_recent_errors`'s `timestamp` field (reachable via `GET /api/audit/errors/recent`) both returned a raw `neo4j.time.DateTime` object, which FastAPI's default JSON encoder can't serialize and instead dumps as nonsensical `__dict__` internals (`"_Date__day": -2`). Found via a broad, deliberate search (not just the two flagged instances) and fixed with one shared helper (`serialize_neo4j_datetime`, `shared/utils/utils.py`) applied consistently at all three call sites, rather than a third inline copy of the same check. `18f84ab`.
3. **Unbounded LLM timeout/retry behavior.** An overnight benchmark run hung for **~3 hours** on a single clause-extraction call before finally raising "The read operation timed out" — no 429, no quota message. Investigation found `get_default_llm()` already had `request_timeout=120` configured; empirically proved (via a real socket that accepts a connection and never responds) that this timeout genuinely fires within its configured window when the process stays running. The likeliest explanation is the machine/session suspending mid-request — no in-process timeout mechanism, of any kind, can fire *during* a full process suspension, since no code executes at all until the OS resumes it. Closed the remaining real gap found along the way regardless: `llm_manager.py` and both `document_processing_service.py` files had no timeout/retry cap at all, unlike `get_default_llm()`. `35574b3`.

**The structural point**: a mock that always returns a canned successful LLM response cannot, by construction, distinguish "the policy check ran and genuinely found nothing" from "the policy check never actually ran." This is not a coverage gap that more mocked tests would have closed — it's a category of bug that is invisible to any test where the LLM call itself is stubbed out, because the entire bug *is* what happens when a real call fails. That is why this needed a live pass with a real, sometimes-failing API in the loop, not a broader mocked suite.

### Auth story rewritten for real JWTs; the walkthrough below reflects it

The walkthrough above still describes how P0–P3 behaved under the old auth mechanism — `tenant_id` as a caller-supplied query parameter and `X-User-Role` as a caller-supplied header, both trusted at face value. That mechanism is gone. Every route that used to read either of them now depends on `get_current_identity` (`backend/governance/auth.py`), which validates a signed JWT and is the *only* source of `tenant_id`/`role` from that point on. Repeating the walkthrough today looks like this:

1. **Register, then get a token.** `POST /api/auth/register` with `{"username", "password", "tenant_id", "role"}` creates a real account — bcrypt-hashed password, stored in Neo4j (`backend/infrastructure/user_repository.py`), a real `User.username` uniqueness constraint behind it. `POST /api/auth/token` with `{"username", "password"}` now verifies those credentials against that stored hash and only *then* returns a signed `access_token` (HS256, `JWT_SECRET_KEY`, 24h expiry) carrying the account's own `tenant_id`/`role` — not whatever a caller asks for. Every other route trusts the signature, not the caller's word. (Registration is deliberately self-service and minimal — no admin-provisioning UI exists yet, so this is currently the only way an account gets created at all; §8 has the honest scope of what this isn't yet.)
2. **Use it as a bearer token on everything else.** `Authorization: Bearer <access_token>` on uploads, analysis, status, search, policy, and audit routes. The old `?tenant_id=` and `X-User-Role` mechanisms are not read anywhere anymore — passing them has no effect at all, verified directly by `test_status_with_valid_token_ignores_caller_supplied_tenant_id_query_param` (`test_tenant_id_required.py`), which sends a request with a *different* `tenant_id` in the query string than the token carries and confirms the query value never reaches the Cypher params.
3. **Cross-tenant isolation is now identity-backed, not just parameter-backed.** The earlier walkthrough's "different tenant_id returns 404" claim was true, but under the old mechanism that was still a caller-supplied value being trusted — proving isolation *given* an honest caller, not against a dishonest one. `test_jwt_auth.py`'s `RealCrossTenantIsolationViaTokenTests` closes that: a genuinely valid, correctly-signed token for tenant B, used against tenant A's real contract_id, gets a clean 404 — not because tenant B typed the "right" id, but because there is no longer any field in the request tenant_id can come from except the token's own verified claim.
4. **`POST /analyze` is now asynchronous.** It returns `202` with `{task_id, status: "PENDING", status_url}` immediately and enqueues a real Celery task (`backend/tasks.py`) instead of blocking on the full multi-clause-extraction-plus-policy-evaluation pipeline in-request. `GET /tasks/{task_id}/status` polls real Celery task state (`PENDING`/`STARTED`/`SUCCESS`/`FAILURE`), and a task that fails mid-pipeline surfaces as a real `FAILURE` state with the underlying error message — not a silently swallowed exception. Only this one operation moved to Celery; `batch-analyze` and the read-path routes (search, status, policy, audit) stay synchronous, since they don't have the same multi-LLM-call runtime that justified the move.

Nothing else about the P0–P3 findings above changed — the encryption, caching, and honest-partial-failure behavior described in this section were re-verified against the new auth layer, not re-tested from scratch.

---

## 6. Closing the README's "Future Enhancements" gap

The original `README.md` listed four items under "Future Enhancements." Rather than infer their status from adjacent work, each was checked directly against running code — file:line, not vibes — and rated Fully Implemented / Partially Implemented / Not Implemented. All four then got real, non-stubbed implementations in this final pass. Before/after, with evidence:

| # | Item | Before this pass | After this pass |
|---|---|---|---|
| 1 | **Distributed Caching** | 🟡 Partial. LLM-extraction responses were cached (P3 item 20), but the actual GraphRAG retrieval path — the P3 item 16 native vector index, `db.index.vector.queryNodes` — had **zero caching**, and a direct read of `EnhancedPrecedentMatcherTool._find_real_precedents` (`enhanced_cuad_tools.py:357`) showed it returning raw, still-encrypted `Clause.content` (P3 item 21 had encrypted it out from under this one read site) straight into a similarity check that could never realistically pass. | 🟢 Real. `enhanced_contract_search_tool.py`'s `get_contracts_multi_level` and `ChunkEmbeddingService.search_similar_chunks` both cache through Redis now, keyed on query text/embedding + `tenant_id` + filters, with a dedicated 10-minute `vector_search` TTL bucket (short relative to the 24h LLM-cache TTL, since a new upload can invalidate a cached result set). The precedent-matcher content is decrypted via the existing `field_encryptor` before use. A third bug found while wiring the cache key: `ChunkEmbeddingService.search_similar_chunks` took **no `tenant_id` at all** — with `document_id` omitted it searched every tenant's chunks — now required and joined through `Document{tenant_id}-[:HAS_CHUNK]->Chunk`. 9 new tests (`test_precedent_matcher_decryption.py`, `test_search_result_caching.py`), including a genuine cross-tenant isolation proof with a positive control. `9649815`. |
| 2 | **Async Batch Workflows** | 🔴 Not implemented. No Celery (or any task queue) existed anywhere in the codebase — `POST /analyze` ran the full multi-clause-extraction-plus-policy-evaluation pipeline synchronously, in-request, empirically 20-25s+. | 🟢 Real. `backend/celery_app.py` + `backend/tasks.py`: `POST /analyze` now enqueues `analyze_contract_task` and returns `202` + `task_id` immediately; `GET /tasks/{task_id}/status` polls real Celery state (`PENDING`/`STARTED`/`SUCCESS`/`FAILURE`), with task-internal failures surfacing honestly rather than swallowed. A `worker` service was added to `docker-compose.yml` (same image as `backend`, `celery worker` entrypoint). Deliberately scoped to this one operation — the only genuinely long-running, multi-LLM-call path — not a wholesale move; `batch-analyze` and all read-path routes stay synchronous. 10 new tests (`test_celery_analysis_task.py`) covering enqueue, real eager-mode execution, status-transition polling, and honest failure surfacing. `a93f029`. |
| 3 | **Multi-Tenant Isolation** | 🟡 Partial. P1 item 6 (§4) got `tenant_id` to "required, not defaulted" — but it was still a bare, caller-supplied query parameter/form field, never verified against any identity. §6's own prior caveat said it plainly: "any caller can still supply any `tenant_id` string it wants." | 🟢 Real. `backend/governance/auth.py`: signed JWT (HS256), issued via `POST /api/auth/token`, validated by `get_current_identity`. Every route that used to read `tenant_id` from a query/form param or role from the unsigned `X-User-Role` header now depends on `requires_permission`/`get_current_identity`, which resolve both exclusively from the token's verified claims. Proven, not assumed: `RealCrossTenantIsolationViaTokenTests` (`test_jwt_auth.py`) uses a genuinely valid, correctly-signed token for tenant B against tenant A's real `contract_id` and gets a clean 404 — because there is no longer any field in the request `tenant_id` can come from except the token itself. `a93f029`. The frontend actually sends this token now too — see below. |
| 4 | **Enhanced Adaptive Learning** | 🔴 Not implemented in any client-visible way. `FeedbackCollector`/`PatternLearner` genuinely stored and learned from historical decisions, but the read side was broken at three independent points: the default execution path (`PlanExecutionEngine`) discarded the enhanced-clause result entirely; `_apply_risk_pattern` wrote to a sibling `learned_risk_adjustment` key that nothing downstream read, instead of the `risk_level` field every consumer actually uses; and `ContractClause` had no field to carry any of it even if the first two were fixed. Each piece "worked" in isolated component tests — the break was only visible end-to-end, which nothing tested. | 🟢 Real. All three breaks closed together: `PlanExecutionEngine` now captures `enhanced_clauses` into `execution_context`; `_apply_risk_pattern` overwrites `risk_level` directly while preserving `original_risk_level`/`pattern_confidence`/`risk_adjustment_pattern_id` for traceability; `ContractClause` carries all four fields end-to-end through the API response. Fixed in passing: `ClauseDetectorTool` never set a baseline `risk_level` at all (silently always `"LOW"`) — new `compute_baseline_risk_level` derives one primarily from real policy-violation severity for that clause (confirmed `CHECK_POLICIES` always precedes `CUAD_MITIGATION` in both plan templates), falling back to a clause-type inherent-risk table. Proven end-to-end, not per-component: a test drives `FeedbackCollector.collect_decision` ×5 → `PatternLearner` reads them back → `AdaptiveAnalyzer` applies the learned pattern → the *next* analysis's `risk_level` genuinely changes. `d867299`. |

Suite growth across this closing pass (`uv run pytest`, matching CI): 241/0/1 → 243/0/1 (item 1) → 252/0/1 (item 4 + its frontend surfacing) → 275/0/1 (items 2-3 + the login work below) — zero regressions at any step.

### Frontend explainability + real login — visible, demo-relevant additions

Two pieces of work exist only in the frontend and didn't exist when this document was first drafted:

- **Explainability UI** (`ClausesDetail.tsx`, part of `d867299`). The API response had carried `clause_id`, `grounded`, `original_risk_level`, `learned_risk_adjustment`, `pattern_confidence`, and `risk_adjustment_pattern_id` since earlier phases, but none of it reached a screen. Now: a Verified/Unverified badge per clause (the Phase 1 anti-hallucination `grounded` signal, previously computed and silently discarded — §4 item 10), an unobtrusive `clause_id` traceability line, and — only when a learned pattern actually applied — a callout sentence ("Risk adjusted from **X** to **Y** based on historical review patterns (N% confidence)"), not a raw JSON dump.
- **Real login flow** (`7350b25`), closing a gap the JWT migration itself created: the frontend had never sent a `tenant_id`/`X-User-Role` of any kind, so once every route required a verified token it would have 401'd on everything. `src/lib/authStore.ts` (a plain, non-React session store — token/tenant_id/role decoded from the token's own claims, persisted to `localStorage`, pub/sub so the non-React `fetchEventSource` chat call can react too) backs a shared `apiFetch` wrapper (`src/lib/apiClient.ts`) that every call site now goes through instead of calling `fetch` directly — one place attaches the bearer token, one place treats a `401` as "log the user out." A minimal `LoginScreen` (tenant_id + role, calling `POST /api/auth/token`) gates the whole app in `App.tsx`, and `Navigation.tsx` shows "Logged in as **{tenant_id}** · **{role}**" with a logout action.

---

## 7. Final live verification: real browser, real login, real async analysis

Every other verification in this document exercised the backend directly (`curl`, `TestClient`, or a live walkthrough that predated the frontend having any auth story at all — §5). This is the one that closes the loop: the actual compiled frontend, in a real Chromium browser, against the full real stack (`docker-compose up`: `backend`, `worker`, `neo4j`, `redis`, `ui`), with **no mocking anywhere in the path** — scripted via Playwright so the sequence is reproducible, not a one-off click-through, with a screenshot captured at each stage.

1. **Login.** Submitted `tenant_id: demo-tenant`, role `ADMIN` on the real `LoginScreen`. `POST /api/auth/token` returned a real signed JWT; the nav bar immediately read "Logged in as **demo-tenant** · **ADMIN**."
2. **Upload.** Dropped `data/Clean_MSA.pdf` (a real contract fixture) through `Upload PDF Contract`, with the bearer token attached automatically by `apiFetch`. Got back a real `contract_id` (`UPLOADED_D0A2BB8A_20260731`) and a "Contract created successfully" confirmation — the upload route's `requires_permission` dependency accepted the token with no query-param/header fallback available to it.
3. **Analyze.** Clicked **Analyze**. This is the real async path end-to-end: `POST /analyze` enqueued the real Celery task and returned `202` immediately; the frontend polled `GET /tasks/{task_id}/status` (via the same `pollTaskStatus` helper added to handle this exact change) until it reached `SUCCESS`. The rendered result was a real Gemini-derived analysis, not fixture data: **Risk Score 45/100 (MEDIUM)**, **1 policy violation**, **7 clauses extracted** — with the explainability UI from §6 live on the page (clause badges, `clause_id` lines) for anyone to inspect during a demo.
4. **Logout.** Clicked **Log out**; the app immediately returned to the login screen, and a direct check of `localStorage` confirmed the session key was actually removed, not just hidden behind a UI state flip.

**Zero browser console errors** (`console.error`/`pageerror`) were recorded across the entire run. This is the strongest evidence in this whole report that the system works as a system: not four independently-tested pieces (JWT validation, Celery task execution, adaptive-learning risk output, explainability rendering) but one continuous, real user action producing a real, correct, explainable result on screen.

### Re-verified after real credential verification replaced claims-only login

The walkthrough above logged in by typing a bare `tenant_id`/role — that mechanism is gone (§6/§8). Repeating the identical Playwright script against the current stack, this time through `LoginScreen`'s registration form:

1. **Register.** Submitted a real `username`/`password`/`tenant_id`/role through the "Create an account" form. `POST /api/auth/register` created a bcrypt-hashed account in Neo4j and returned `201`; the UI switched back to the sign-in form with an "Account created" confirmation.
2. **Log in with those exact credentials.** `POST /api/auth/token` verified the username+password against the stored hash and returned a signed JWT carrying that account's own `tenant_id`/role — the nav bar read "Logged in as **demo-tenant-e2e** · **ADMIN**," derived from the token's claims, unchanged from before.
3. **Upload → Analyze → results**, identical to the original run: `Clean_MSA.pdf` uploaded, real Celery task enqueued and polled to `SUCCESS`, real Gemini-derived results rendered — **Risk Score 95/100 (CRITICAL)**, **3 policy violations**, **7 clauses extracted** (a different score than the original run's 45/MEDIUM, expected: this pass hit the live Gemini API fresh rather than a cached result).
4. **Logout**, confirmed via the same direct `localStorage` check as before.

**Zero browser console/page errors** on this run either. The only thing that changed end-to-end is *how* the session began (real verified credentials instead of a typed claim) — everything downstream (`get_current_identity`, `requires_permission`, the Celery task, the explainability UI) needed no changes at all, which is exactly what the extension point documented in `governance/auth.py` was designed to guarantee.

---

## 8. Honest current-state assessment

Deliberately scoped-out or still-partial items a reader should know about — not forgotten, consciously not done, or found and left for a future pass:

- **RESOLVED — `tenant_id` now has a real authenticated-identity layer.** *Was:* P1 item 6 scoped down to "reject if missing" (`ae7d400`) rather than the full fix; any caller could supply any `tenant_id` string it wanted, with no verification it was entitled to that tenant. *Now:* `tenant_id`/`role` come exclusively from a validated, signed JWT (`backend/governance/auth.py`, `a93f029`) — no route reads either from a client-supplied query/form/header value anymore, and cross-tenant isolation is proven against a token that's genuinely valid *for the wrong tenant*, not just a caller-supplied mismatch (§6/§7). Kept, deliberately: the new bullet below.
- **RESOLVED — token issuance now verifies real credentials.** *Was:* `POST /api/auth/token` accepted a bare `tenant_id` + role and signed a token for it — anyone who could reach the login screen could mint a valid token for *any* tenant_id they typed in. *Now:* `POST /api/auth/register` creates a real account (`backend/infrastructure/user_repository.py`) with a bcrypt-hashed password behind a real `User.username` uniqueness constraint; `POST /api/auth/token` verifies a username+password against that stored hash before issuing anything, embedding the account's own `tenant_id`/`role` rather than a caller-supplied claim. `get_current_identity`/`requires_permission` (the validation side) were deliberately left untouched by this change — the extension point documented from the start meant only the issuance layer (`api/auth_api.py`) needed to change. Kept, deliberately: the new bullet below.
- **Registration is minimal and self-service, not a full IdP.** No org-admin invite flow, no SSO, no password reset, no MFA, no email verification — `POST /api/auth/register` is currently the *only* way any account gets created, which is appropriate for a capstone/demo scope but not a production provisioning story. A real IdP (Auth0/Okta/Cognito) can still replace just the issuance routes later without touching `get_current_identity` or anything that depends on it, as documented in `governance/auth.py`'s module docstring.
- **RESOLVED — `ChainOfThoughtAgent`/`ReACTAgent` and the whole pattern-analysis system were removed, not upgraded.** *Was:* deterministic f-string templating dressed up as reasoning, reachable only via the traditional-workflow fallback (`PlanExecutionEngine`, the real default, has no pattern-analysis step type at all). *Investigated whether to build real LLM-backed reasoning instead and found the case against it stronger than expected:* `use_planning` defaults to `True` at every layer including the frontend, which never overrides it — this path is not merely low-traffic, it is unreachable outside a hand-crafted API call. Its output (`pattern_used`/`pattern_analysis`) never influenced `risk_level`, `violations`, or anything else downstream, even when manually triggered — a pure dead side-channel, never rendered in the frontend either. `ChainOfThoughtAgent`'s policy check had also drifted into a third, independent hardcoded keyword matcher (`prohibited_terms = ['unlimited liability', ...]`) duplicating what `PolicyEvaluationService` already does correctly everywhere else (P2 item 11) — a "minimal" real-reasoning fix would have also needed reconciling with that engine to avoid producing internally-inconsistent results. Same rationale and evidence shape as the Supervisor removal (§8 above). *Removed, cascading beyond the three explicitly-named classes once their hard dependencies were traced*: `ReACTAgent`, `ChainOfThoughtAgent`, `PatternSelector`, the `_pattern_analysis` workflow node and its edges/state fields (`pattern_used`/`pattern_analysis` in `IntelligenceState`) - plus `BasePatternAgent` (orphaned once its only two subclasses were gone) and `PatternOrchestrator`/`PatternOrchestratorFactory` and the standalone `api/patterns_api.py` route it powered, both confirmed unreachable from the frontend and confirmed to contain a real bug of their own (`/react`/`/chain-of-thought` called `agent.process(...)`, a method that never existed on either class - only `.execute()` does). `AdvancedRAGAgent` was not part of this finding and is untouched; `test_pattern_integration.py` narrowed to just its coverage (`TestAdvancedRAGAgent`), with `TestReACTAgent`/`TestChainOfThoughtAgent`/`TestPatternSelector`/`TestOrchestratorIntegration`/`TestLoggingIntegration`/`TestPatternOrchestrator` removed alongside the code they tested. `docs/REACT_COT_INTEGRATION.md` (a dedicated feature doc claiming "✅ Production Ready" and a fabricated "+10-20% Accuracy Gain" that was never true even before removal, since the pattern's output never reached any real field) deleted outright rather than corrected in place.
- **RESOLVED — the 2 known-broken async smoke-test files are gone, not just excluded.** *Was:* `test_ai_patterns.py`/`test_policy_system.py`, `async def` scripts with zero `assert` statements calling real Agent classes with no LLM mocking, excluded from CI via `--ignore` (item 22). *Now:* both deleted; their coverage was checked against the rest of the suite first, not assumed. `test_ai_patterns.py`'s `ReACTAgent`/`ChainOfThoughtAgent` coverage was already fully duplicated by `test_pattern_integration.py`; its `AdvancedRAGAgent`/`PatternOrchestrator` coverage was genuinely new and got real tests added there instead (a fake-graph-backed `TestAdvancedRAGAgent`, a fake-sub-agent `TestPatternOrchestrator` — the latter, along with `ReACTAgent`/`ChainOfThoughtAgent`/`PatternOrchestrator` themselves, was subsequently removed entirely; see the pattern-analysis-removal entry above). `test_policy_system.py`'s policy-repository-search coverage was already duplicated by `test_vector_index_search.py`, and its "dynamic policy loading" CoT test was already exercised unconditionally by `test_pattern_integration.py`'s existing risk-assessment test; its genuinely uncovered pieces (`PolicyChunkingStrategy`, `PolicyChunkingAgent`, `ChunkingFactory`'s policy registration) became `test_policy_chunking.py`. Writing real assertions surfaced two real, previously-silent bugs the old zero-assert version had been masking: `PolicyChunkingStrategy` was missing `IChunkingStrategy`'s abstract `get_chunk_size()` and could never be instantiated at all — meaning `ChunkingFactory.create_strategy('policy')`, the real path behind `POST /api/policies/upload`'s first step, has been raising `TypeError` this entire time; and `get_available_strategies()` never listed `"policy"` as available even though `create_strategy("policy")` worked. Both fixed. CI's `--ignore` flags are gone; `uv run pytest tests/ -q` now collects and passes the whole suite with no exclusions.
- **Risk-relevant clause extraction recall is meaningfully lower than metadata extraction** — the single most important accuracy finding of this engagement (§4 item 12): 0.32 average F1 across 36 risk-relevant categories vs 0.75 across the 5 metadata columns, full 497-contract scale. This is measured now, not hidden, but it is not fixed — the platform's core risk-flagging value proposition still has real, quantified accuracy gaps, especially in 20 of 36 categories scoring below 0.30 F1.
- **RESOLVED — `convert_neo4j_date`'s zero-padding bug is fixed.** *Was:* `f"{value.year}-{value.month}-{value.day}"` rendered e.g. January 5th as `"2024-1-5"` instead of `"2024-01-05"` — a malformed-but-present string, not a crash, affecting `effective_date`/`end_date` in the search-tool paths (`shared/utils/search_strategies.py`, `contract_search_tool.py`, `enhanced_contract_search_tool.py`). *Now:* zero-padded via `f"{value.year:04d}-{value.month:02d}-{value.day:02d}"`, true ISO 8601. Confirmed nothing downstream relied on the malformed shape before fixing: every call site uses it only as the final output-formatting step before returning to the API response, never re-parsed, compared, or sliced afterward, and no test asserted on the unpadded format. 7 new regression tests (`ConvertNeo4jDateZeroPaddingTests`, `test_neo4j_datetime_serialization.py`) cover single-digit month/day in both positions, dict/list recursion, and a guard against the opposite mistake (double-padding an already-2-digit value).
- **RESOLVED — the Supervisor orchestration path was removed as dead weight, not wired in.** *Was:* three parallel orchestration systems (traditional LangGraph workflow, `PlanExecutionEngine` — the real default, and `SupervisorAgent`, reachable only via a separate route) with no decision made on the Supervisor path's fate. *Investigation found it confirmed-dead in three independent places, not just "unused":* `PolicyWorkflowOrchestrator`/`PolicyWorkflowSupervisor` (the real policy-upload path, via `PolicyService`) each constructed a `SupervisorAgent` in `__init__` and never called `coordinate_workflow` — both always ran their own manual step loop instead. The standalone `POST /api/supervisor/workflow/execute` route constructed a *fresh* `SupervisorAgent` (and fresh `CircuitBreakerManager`) on every single request, so its circuit breaker's failure count could never accumulate past 0 — non-functional by construction, not just untested; the paired `GET /workflow/{id}/status` route had the identical bug, so it always returned "not found" for any id. Its one genuinely distinct capability (output-quality grading, not just success/failure) was computed and logged but never gated even within the Supervisor's own code — inert. A confirmed bug in its adapter layer (`analyze_contract_by_id("gemini-2.5-flash")` passing the model name positionally into the `tenant_id` slot) had sat undetected, further evidence this path had never actually run for real. *Removed*: `supervisor_agent.py`, `circuit_breaker.py`, `retry_manager.py`, `quality_manager.py`, `quality_gates.py`, `quality_scorer.py`, `validation_strategies.py`, `consensus_manager.py`, `consensus_strategy.py`, `message_bus.py`, `workflow_engine.py`, `agent_factory.py`, `base_adapter.py`, `adapters.py`, `factory.py`, `workflow_context.py`, `error_recovery.py` (17 files), `api/supervisor_api.py` and its `main.py` registration, and the dead `self.supervisor = SupervisorAgent(...)` lines in both orchestrator classes. *Kept, confirmed load-bearing*: `agents/supervisor/interfaces.py`'s shared `IAgent`/`AgentContext`/`AgentResult`/`ValidationResult` protocol and `agent_registry.py`'s `AgentRegistry` — `PolicyChunkingAgent`/`PolicyExtractionAgent`/`PolicyComplianceAgent` (the real policy-upload agents) genuinely subclass/use these independent of `SupervisorAgent`'s coordination logic, verified by running `PolicyWorkflowOrchestrator.process_policy_document` end-to-end post-removal. Two remaining orchestration systems now, both real: the traditional LangGraph workflow and `PlanExecutionEngine` (the default) — no longer three, one of which was fake.
- **`correlation_id`/`trace_id` bridging is a capability, not a wire-up.** All 4 MCP tool functions accept an optional `correlation_id` now, but nothing in this codebase currently calls from FastAPI into the MCP server in-process — there is no existing call chain to actually thread a live value through yet.
- **No real secrets manager.** `ENCRYPTION_KEY` is environment-variable-sourced with a loudly-logged insecure dev-only fallback if unset. `IKeyProvider` exists specifically so a real provider can be swapped in later without touching any encrypt/decrypt call site, but none is built, since no deployment target has been chosen yet.
- **RESOLVED — `FieldEncryptor.decrypt()` no longer falls back to returning its input unchanged on failure.** *Was:* framed as an explicit safety net for legacy plaintext during a migration window, but flagged even then as "don't rely on this to signal tampering" — the one inconsistency in an otherwise rigorous confidentiality story. *Now:* there was no real legacy-plaintext migration actually in flight (every write site has encrypted before persisting since each field was introduced, confirmed by inspection, not assumed), so the fallback was removed outright rather than "tightened" - `decrypt()` raises a new `DecryptionError` on any failure (bad base64, wrong key, or a tampered ciphertext whose AES-GCM auth tag fails to verify), proven by a new test that flips a byte in a real ciphertext's auth tag and confirms it raises rather than silently handing back garbage or the original plaintext. Three existing test fixtures that stored raw plaintext where real ingestion would have stored ciphertext (`test_precedent_matcher_relationship.py`, `test_search_result_caching.py` x2) were only passing *because* of the old silent fallback - fixed to store real `field_encryptor`-encrypted content, matching what real ingestion actually writes.
- **The Chunk-content `CONTAINS`-fallback search path's `total_count` is a bounded approximation** (`CHUNK_TEXT_SEARCH_CANDIDATE_LIMIT=500`), not a true unbounded count — the same tradeoff class already accepted for `VECTOR_SEARCH_OVERFETCH`, and it only affects a fallback path (the primary path is the real vector index).
- **PII detection is pattern-based** (the same `PIIEngine` used by the chat path), not a general/ML-based classifier — it covers a defined set of patterns, not every conceivable PII shape.
- **The gemini-2.5-flash risk-category confirmation sample is incomplete** (5/90 as of this report), gated on the free-tier per-minute/per-day quota. The daily recurring job continues to make incremental progress; this is a data-completeness gap, not an unresolved bug.

---

## 9. If continuing this work

In rough priority order, given everything found above:

1. **Complete the gemini-2.5-flash risk-category confirmation sample** (currently 5/90) once quota allows, to know whether the production model meaningfully closes the risk-category recall gap the way it did for metadata (§3).
2. **Grow self-service registration into a real IdP story** — org-admin invites (rather than open self-registration), SSO, password reset, MFA, email verification (§8). `get_current_identity`/`requires_permission` need no changes for any of this; it's still purely an issuance-layer swap, the same extension point already used once to add real credential verification in the first place.
3. **~~Decide the fate of the 3 parallel orchestration systems~~ — done: the dead Supervisor path was removed (§8).** Two real systems remain (traditional LangGraph workflow, `PlanExecutionEngine`); if consolidating those two further is ever worth doing, `PlanExecutionEngine` is the one to keep given it's the actual default.
4. **~~Convert or delete the 2 excluded async smoke-test files~~ — done (item 22, §8).** Both deleted; their genuinely-new-coverage pieces became real tests in `test_pattern_integration.py`/`test_policy_chunking.py`, and the process surfaced/fixed two real, previously-silent bugs in `PolicyChunkingStrategy`/`ChunkingFactory`.
5. **~~Extend real LLM-backed reasoning to `ChainOfThoughtAgent`/`ReACT`~~ — decided against; removed instead (§8).** Investigated first: unreachable in real usage, dead-side-channel output even when triggered, and architecturally drifted from the rest of the system. Building real reasoning would have been disproportionate effort for a path with confirmed-zero real-world value.
6. **Stand up a real secrets-manager-backed key provider** once a deployment target is chosen (`IKeyProvider` is already the extension point).
7. **~~Fix `convert_neo4j_date`'s zero-padding bug~~ — done (§8).**
8. **Wire `correlation_id` end-to-end** once a real FastAPI → MCP in-process call path exists to carry it.
9. **Push the risk-category benchmark further**: investigate the 20-of-36 categories scoring below 0.30 F1 individually — some (e.g. Price/Volume Restriction, Joint IP Ownership) may be near-zero due to genuine corpus rarity rather than model failure, which changes the remediation (more ground truth, not a better prompt).

---

## 10. Production-readiness audit

A genuine "could this actually be deployed and run as a real service" audit, distinct from demo-readiness (already live-verified — §7). Scoped across 6 areas: deployment/infra, security, scalability/reliability, observability, configuration/environments, and AI/ML engineering depth. Findings rated by real production severity; the 6 rated critical were fixed this pass. Everything below is grounded in file:line, matching every other audit in this engagement.

### Critical findings — fixed

1. **`LLMUsageTracker` was in-process, blind to the operation that generates almost all real spend.** *Was:* a plain in-process dict (`shared/monitoring/llm_usage_tracker.py`) read by `GET /api/monitoring/llm-usage`, served by the `backend` container — but the LLM calls it records (`llm_extraction_service.py`, `policy_evaluation_service.py`) run inside the separate `worker` Celery container since the async-analysis migration (§6). Two OS processes, two copies of memory: the cost dashboard reported near-zero for the real workload. *Now:* counters moved to Redis (the same `cache` singleton already deployed for caching), shared across every process that imports the module. `InMemoryCache` (the no-real-Redis fallback used in dev/tests) gained `incr`/`incrby`/`incrbyfloat`/`sadd`/`smembers` so the tracker's code doesn't need to know which backend it's talking to. Proven concretely, not just asserted: `test_llm_usage_tracker_redis_backed.py`'s `CrossProcessVisibilityTests` constructs two independent `LLMUsageTracker` instances sharing only a backing store (modeling backend vs. worker) and confirms usage recorded by one is visible to the other.
2. **CORS was `allow_origins=["*"]` with `allow_credentials=True`.** *Was:* unchanged from the original audit — Starlette's `CORSMiddleware` reflects the actual request `Origin` back in this combination (can't literally emit `*` with credentials per spec), so any origin could make credentialed calls. *Now:* `main.py`'s `_get_cors_origins()` stays permissive only when `is_production()` is false (dev convenience preserved); in production it requires an explicit, comma-separated `CORS_ALLOWED_ORIGINS` env var and fails closed (empty allow-list, not `*`) if unset, with a loud warning.
3. **`GET /debug/contracts` was fail-open and leaked cross-tenant data.** *Was:* `os.getenv("ENVIRONMENT", "development") != "production"` — unset defaulted to *shown*; the query itself (`MATCH (c:Contract) RETURN ...`) had zero tenant filtering, so any caller with `VIEW_AUDIT` permission (any role, any tenant) saw every tenant's contracts. *Now:* `_debug_routes_enabled()` requires `ENVIRONMENT` to be explicitly set to something other than `"production"` — unset means hidden (fail closed); `docker-compose.yml`'s `backend` service now sets `ENVIRONMENT=development` explicitly so local dev doesn't silently lose the routes. Both routes are also now tenant-scoped via the validated JWT identity, so even an intentionally-enabled debug route can't cross tenant boundaries. Proven with the exact scenario requested: `test_debug_routes_tenant_isolation.py` hits the real route through a `TestClient` with two tenants against a tenant-scoped fake graph and confirms tenant A never sees tenant B's contract (and vice versa).
4. **Insecure JWT/encryption defaults could silently ship to production.** *Was:* both `governance/auth.py`'s `_get_secret_key()` and `infrastructure/encryption.py`'s `EnvKeyProvider.get_key()` fell back to a hardcoded, published-in-this-repo default with only a logged warning, in any environment. *Now:* `validate_production_secret()`/`validate_production_key()` hard-fail (`RuntimeError`) at application startup (`main.py`'s `lifespan`, before the app accepts a single request) when `is_production()` is true and either secret is unset or still equal to its dev default; both no-op entirely outside production. Proven end-to-end, not just unit-level: `test_production_secret_validation.py`'s `ApplicationStartupHardFailsInProductionTests` actually constructs the real FastAPI app and triggers real `TestClient` startup with `ENVIRONMENT=production` and no secrets set, confirming the app genuinely refuses to start — and confirms it starts cleanly once real secrets are supplied.
5. **README's "Future Enhancements" list claimed 4 shipped, tested features didn't exist.** *Was:* Distributed Caching, Async Batch Workflows, Multi-Tenant Isolation, and Enhanced Adaptive Learning were all still listed as future work, despite being fully implemented, tested, and documented in §6 several turns earlier — actively underselling finished work. *Now:* rewritten as "✅ Implemented Since the Original Capstone Scope" with a pointer to §6 for full evidence; "Future Enhancements" now lists only what's genuinely still open. Fixed two adjacent, equally-stale claims found in passing: the Redis usage note (previously "disabled, overkill for capstone" — Redis is enabled by default and backs caching/Celery/usage-tracking) and a broken document structure (the closing tagline and horizontal rule had landed in the middle of the numbered Quick Start list, orphaning steps 5-6 after it).
6. **No automated dependency vulnerability scanning anywhere.** *Was:* no Dependabot config, no `pip-audit`/`npm audit` step in CI. *Now:* a new `security` job in `.github/workflows/ci.yml` runs `pip-audit` (via `uvx`, against a `uv export`-generated lockfile) for the backend and `npm audit` for the frontend. **Deliberately report-only** (`continue-on-error: true`), not blocking — confirmed, not assumed, why: running both locally before wiring them in surfaced a real, untriaged baseline (99 backend findings via `pip-audit`, mostly in transitive dependencies like `pypdf`/`starlette`/`urllib3`; 12 frontend findings — 3 low, 1 moderate, 8 high — all in the Vite dev-server/build-toolchain chain). Making this blocking on day one would turn CI red for reasons unrelated to whatever change triggered it — the same reasoning this file's own `lint` job already models (E9/F821 is blocking *because* it was audited clean first; the full ruff report stays non-blocking because that backlog was never triaged). Confirmed and documented in the workflow comment: the frontend findings are scoped to `vite dev`/`vite build`'s own tooling, never present in the shipped production image (`frontend/Dockerfile`'s `production` stage is nginx serving a static build — no Node, no Vite, no `node_modules` at all).

Full suite: 317 passed / 0 failed / 1 skipped (24 new tests), up from 293/0/1. Lint clean. CI green.

### Findings not fixed this pass (real, rated, deliberately out of scope)

Reported in full during the audit; the user scoped this pass to the 6 critical items above. Still open:

- **Important**: two confirmed per-request Neo4j connection leaks (`api/enhanced_document_upload.py`'s `embedding-status` route, `EnhancedDocumentProcessingService.__init__`) construct a fresh, never-closed driver instead of reusing the existing module-level singleton; worker graceful-shutdown gap (no `stop_grace_period`, no `task_acks_late` — a routine deploy can silently drop an in-flight analysis); `/api/monitoring/health` never checks Neo4j and never returns a non-2xx status on failure; no Prometheus-compatible `/metrics` endpoint; `PROMPT_VERSION = "v1"` has never been bumped despite prompt changes since introduction; grounding/hallucination checks fire silently with no tracked rate; cost and latency tracking don't overlap on the operations that matter (p95 exists but isn't applied to extraction/policy-evaluation calls); no evaluation report artifact consolidating the CUAD methodology and benchmark results; no security headers (CSP/HSTS/X-Frame-Options); no API-level rate limiting on `/api/auth/token`/`/api/auth/register`.
- **Nice-to-have**: `api/routes/production.py`'s `create_production_router()` is dead code (imported, never called); `/api/monitoring/system-info`'s `psutil` import always fails (not a real dependency); `docker-compose.yml` has no `restart:`/resource-limit policy (acceptable — it's an explicitly dev-only file).
- **Blocked on a deployment decision, not a code gap**: TLS/HTTPS termination, Neo4j backup/DR, a real secrets manager, horizontal-scaling infra. All require a chosen target (domain/cert strategy, backup destination, provider) before anything concrete can be built — same category as the encryption-key/secrets-manager scope questions handled earlier in this engagement.
- **Investigated and deliberately not built**: a precision@k retrieval-quality metric for GraphRAG/precedent-matching. Genuinely absent, but building it would require a real relevance-labeled query set (which contracts are actually true precedents for a sample of queries) — new ground-truth labeling work, not a code change. Fabricating a number without real labels would violate this engagement's evidence discipline, so it wasn't built.
