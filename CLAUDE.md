# Claude Code adapter

Read and follow `AGENTS.md`; it is the repository's authoritative, tool-neutral instruction file. Then read the active task contract and the sources assigned by `docs/SOURCE_OF_TRUTH.md` before editing.

Claude-specific reminders:

- Treat prior conversation and compacted summaries as non-authoritative handoff hints.
- Begin tool switches with the read-only audit required by `AGENTS.md`.
- Keep Claude plans/checkpoints synchronized with the repository task contract; do not create a competing rules document.
- Report exact checks not run and preserve all pre-existing worktree changes.
